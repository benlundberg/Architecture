<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
	xmlns="http://xamarin.com/schemas/2014/forms" 
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
	xmlns:local="clr-namespace:Architecture.Controls;assembly=Architecture"
	x:Class="Architecture.Controls.FloatingEntry">
	<Grid Margin="0, 8">
		<Grid.RowSpacing>
			<OnPlatform x:TypeArguments="x:Double">
		      <OnPlatform.iOS>
		        16
		      </OnPlatform.iOS>
		      <OnPlatform.Android>
		        0
		      </OnPlatform.Android>
		    </OnPlatform>
		</Grid.RowSpacing>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="1" />
		</Grid.RowDefinitions>
		<Label
			x:Name="HiddenLabel"
			FontSize="12"
			IsVisible="false"
			Margin="0" />
		<local:BorderlessEntry
			x:Name="EntryField"
			Text="{Binding Text, Mode=TwoWay}"
			Margin="0, 12, 0, 0" 
			HorizontalOptions="FillAndExpand" />
		<BoxView 
			x:Name="BottomBorder"
			Grid.Row="1"
			HeightRequest="1"
			Margin="0"
			BackgroundColor="Gray"
			HorizontalOptions="FillAndExpand" />
		<BoxView 
			x:Name="HiddenBottomBorder"
			Grid.Row="1"
			HeightRequest="1"
			Margin="0"
			HorizontalOptions="Center" />
	</Grid>
</ContentView>
