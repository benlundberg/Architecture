<?xml version="1.0" encoding="UTF-8"?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:controls="clr-namespace:Architecture.Controls"
    x:Class="Architecture.Controls.ValidationEntry"
    x:DataType="controls:ValidationEntry"
    x:Name="this"
    Visual="Material">

    <StackLayout>
        <Entry
            Placeholder="{Binding Placeholder, Source={x:Reference this}}"
            PlaceholderColor="{Binding PlaceholderColor, Source={x:Reference this}}"
            Text="{Binding Value.Value, Source={x:Reference this}, Mode=TwoWay}"
            TextColor="{Binding TextColor, Source={x:Reference this}}"
            IsPassword="{Binding IsPassword, Source={x:Reference this}}"
            Keyboard="{Binding Keyboard, Source={x:Reference this}}"
            Unfocused="Entry_Unfocused" />
        <Label 
            Text="{Binding Value.Error, Source={x:Reference this}}"
            TextColor="IndianRed"
            IsVisible="{Binding Value.IsValid, Source={x:Reference this}, Converter={StaticResource InvertedBoolConv}}"  />
    </StackLayout>
    
</ContentView>